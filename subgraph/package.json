{
  "name": "deepFamily-subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020 deepFamily/subgraph",
    "deploy-local": "graph deploy --product hosted-service deepFamily/subgraph || graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 deepFamily/subgraph",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.64.1",
    "@graphprotocol/graph-ts": "^0.33.0",
    "assemblyscript": "^0.27.37",
    "matchstick-as": "^0.6.0"
  }
}
