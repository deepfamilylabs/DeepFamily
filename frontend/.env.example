# Frontend environment configuration
# - Local dev: `npm run config:local` generates `frontend/.env.local` from local deployments.
# - Manual: copy this file to `frontend/.env` (or `.env.local`) and fill in values.

# =============================================================================
# REQUIRED
# =============================================================================

# Generated by `npm run config:local`:
# - VITE_RPC_URL
# - VITE_CONTRACT_ADDRESS
# - VITE_ROOT_PERSON_HASH (+ per-language variants)
# - VITE_ROOT_VERSION_INDEX (+ per-language variants)

VITE_RPC_URL=http://127.0.0.1:8545
VITE_CONTRACT_ADDRESS=0x...
VITE_ROOT_PERSON_HASH=0x...
VITE_ROOT_VERSION_INDEX=1

# =============================================================================
# OPTIONAL: Multi-language Roots (populated by `npm run config:local`)
# =============================================================================

# VITE_ROOT_PERSON_HASH_EN=0x...
# VITE_ROOT_VERSION_INDEX_EN=1
# VITE_ROOT_PERSON_HASH_ZH=0x...
# VITE_ROOT_VERSION_INDEX_ZH=1

# =============================================================================
# OPTIONAL: Network
# =============================================================================

# Optional: Multicall contract address (enables batching many read-only calls into fewer RPC requests).
# Leave unset unless your target network has a deployed Multicall.
# VITE_MULTICALL_ADDRESS=0x...

# =============================================================================
# OPTIONAL: IPFS Gateways (runtime; used by UI and should match your CSP allowlist)
# =============================================================================

# Comma/space/newline-separated base URLs, e.g.:
# VITE_IPFS_GATEWAY_BASE_URLS="https://ipfs.io/ipfs/ https://dweb.link/ipfs/"
# VITE_IPFS_GATEWAY_BASE_URLS=

# =============================================================================
# OPTIONAL: Performance (tune down for public RPCs)
# =============================================================================

# VITE_DF_PARALLEL=2
# VITE_DF_PAGE_SIZE=10
# VITE_DF_ENDORSE_BATCH=10
# VITE_DF_MAX_DEPTH=20
# VITE_DF_HARD_NODE_LIMIT=10000

# =============================================================================
# OPTIONAL: Branding
# =============================================================================

# VITE_BRAND_BADGE=alpha

# =============================================================================
# OPTIONAL: Debug
# =============================================================================

# VITE_SHOW_DEBUG=1
# VITE_STRICT_CACHE_ONLY=true
# VITE_FORCE_ENV_CONFIG_SYNC=true
# VITE_SHOW_DEDUPLICATE_TOGGLE=1

# =============================================================================
# OPTIONAL: CSP (Vite Server/Preview; read by `vite.config.ts`, server-side only)
# =============================================================================

# Write CSP violation reports to this file (JSONL). Defaults to `.csp-report.<mode>.jsonl` during `npm run csp:scan`.
# DEEP_CSP_REPORT_FILE=

# Preview/prod: enforce CSP instead of Report-Only.
# DEEP_CSP_ENFORCE=1

# Add extra allowed origins (space-separated).
# Example: DEEP_CSP_CONNECT_SRC="https://rpc.ankr.com https://my.gateway.example"
# Note: `connect-src` already includes the origin of `VITE_RPC_URL` automatically.
# DEEP_CSP_CONNECT_SRC=
# Example: DEEP_CSP_IMG_SRC="https://my.gateway.example"
# DEEP_CSP_IMG_SRC=

# Auto-include built-in RPC/IPFS origins (from `src/config/networks.ts` and `src/config/ipfs.ts`).
# DEEP_CSP_INCLUDE_NETWORK_PRESETS=0
# DEEP_CSP_INCLUDE_IPFS_GATEWAYS=0
# Preview audit: disallow inline style attributes (likely breaks until refactored)
# DEEP_CSP_STYLE_ATTR_NONE=1

# =============================================================================
# OPTIONAL: CSP Scan (Playwright)
# =============================================================================

# CSP_SCAN_MODE=preview|dev
# CSP_SCAN_HEADLESS=1
# CSP_SCAN_HOST=127.0.0.1
# CSP_SCAN_PORT=4173
# CSP_SCAN_BASE_URL=http://127.0.0.1:4173
# CSP_SCAN_SKIP_BUILD=1
# CSP_SCAN_FAIL_ON_REPORT=1
# CSP_SCAN_EXECUTABLE_PATH="/mnt/c/Program Files/Google/Chrome/Application/chrome.exe"
# CSP_SCAN_CHROME_CHANNEL=chrome
