# Frontend Environment Configuration
# 
# LOCAL DEVELOPMENT:
#   Run `npm run config:local` to auto-configure from deployment
#
# MANUAL SETUP:
#   Copy this file to .env and update with your values

# =============================================================================
# REQUIRED: Basic Configuration
# =============================================================================

# RPC endpoint for blockchain connection
VITE_RPC_URL=http://127.0.0.1:8545

# DeepFamily smart contract address
VITE_CONTRACT_ADDRESS=0x...

# Root person hash (Genesis person in the family tree)
VITE_ROOT_PERSON_HASH=0x...

# Root version index (usually 1)
VITE_ROOT_VERSION_INDEX=1

# =============================================================================
# OPTIONAL: Network Configuration
# =============================================================================

# Chain ID to prevent network detection (uncomment if needed)
# Local Hardhat: 31337
# Conflux eSpace Testnet: 71
# Conflux eSpace Mainnet: 1030
# VITE_CHAIN_ID=31337

# Network name for display purposes
# VITE_NETWORK_NAME=localhost

# Multicall contract address (if available)
# VITE_MULTICALL_ADDRESS=0x...

# =============================================================================
# OPTIONAL: Performance Tuning
# =============================================================================

# Reduce these values for public RPCs to avoid rate limits
# Default values are optimized for local development

# Number of parallel requests (default: 6)
# VITE_DF_PARALLEL=2

# Page size for data fetching (default: 25)
# VITE_DF_PAGE_SIZE=10

# Endorsement batch size (default: 40)
# VITE_DF_ENDORSE_BATCH=10

# Maximum tree depth (default: 30)
# VITE_DF_MAX_DEPTH=20

# Hard limit on number of nodes (default: 20000)
# VITE_DF_HARD_NODE_LIMIT=10000

# =============================================================================
# OPTIONAL: Debug and Development
# =============================================================================

# Enable debug mode in TreePage (shows additional info)
# VITE_SHOW_DEBUG=1

# Enable strict cache-only mode (prevents new data fetching)
# VITE_STRICT_CACHE_ONLY=true

# =============================================================================
# PRODUCTION EXAMPLES
# =============================================================================

# Conflux eSpace Testnet Configuration:
# VITE_RPC_URL=https://evmtestnet.confluxrpc.com
# VITE_CONTRACT_ADDRESS=0x63ea5897C88c9Dac09c3d5Af7a55f1442F08A7E9
# VITE_CHAIN_ID=71

# Performance settings for public RPCs:
# VITE_DF_PARALLEL=2
# VITE_DF_PAGE_SIZE=10
# VITE_DF_ENDORSE_BATCH=10